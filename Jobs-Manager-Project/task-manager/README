# All learning
 -CRUD application
   a.app.get
   b.app.getAll
   c.app.create
  d.app.update
  e.app.delete
-AsyncWrapper and Error handling
  Basically we create asyncWrapper middleWare to refactor our code, so that we do not have to write try and catch again and again




